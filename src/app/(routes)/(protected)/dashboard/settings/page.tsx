import ProfileSettings from "@/features/interviews/interviewer-dashboard/profile-settings";

const ProfileSettingsPage = () => {
   return <ProfileSettings />;
};

export default ProfileSettingsPage;
